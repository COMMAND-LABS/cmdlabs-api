# TLDR

Granting and revoking super admin perms

##

`python -m scripts.super_admin --apply --grant --email <EMAIL_HERE>`

##

`python -m scripts.super_admin --apply --revoke --email <EMAIL_HERE>`

`python -m scripts.super_admin --apply --grant --email tad@cmdlabs.io`

`python -m scripts.super_admin --apply --revoke --email tad@cmdlabs.io`